<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.s23001792.thiriposa">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Microphone permission for Voice Chat -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_thiri"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_thiri"
        android:supportsRtl="true"
        android:theme="@style/Theme.Thiriposa">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBhiBspdv3ufHAEFk1Wn8VEJe3j9qzrrEo" />

        <!-- Main launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login / Signup / Forgot Password -->
        <activity android:name=".LoginActivity" android:exported="true" />
        <activity android:name=".SignupActivity" android:exported="false" />
        <activity android:name=".ForgotPasswordActivity" android:exported="false" />

        <!-- Role-based Home Pages -->
        <activity android:name=".MotherHomePage" android:exported="false" />
        <activity android:name=".NurseHomePage" android:exported="false" />
        <activity android:name=".MOHHomePage" android:exported="false" />

        <!-- Appointment screens -->
        <activity android:name=".AppointmentSendActivity" android:exported="false" />
        <activity android:name=".AppointmentReceiveActivity" android:exported="false" />

        <!-- Health screens -->
        <activity android:name=".HealthUpdateActivity" android:exported="false" />
        <activity android:name=".HealthReportActivity" android:exported="false" />

        <!-- Map Activity with Location/GPS -->
        <activity android:name=".MapActivity" android:exported="false" />

        <!-- Packet screens -->
        <activity android:name=".PacketUpdate" android:exported="false" />
        <activity android:name=".PacketReport" android:exported="false" />
        <activity android:name=".PacketReportData" android:exported="false" />

        <!-- BMI data screen -->
        <activity android:name=".BmiData" android:exported="false" />

        <!-- Voice Chat Activity -->
        <activity android:name=".VoiceChatActivity" android:exported="false" />

    </application>

</manifest>
